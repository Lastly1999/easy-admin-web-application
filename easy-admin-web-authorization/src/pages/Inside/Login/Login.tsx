import {defineComponent} from "vue"

/**
 * 系统授权内部登录页
 */
const Login = defineComponent({
    setup() {

        const loginAction = () => {
            // todo
        }

        return () => (
            <div>
            </div>
        )
    }
})

export default Login