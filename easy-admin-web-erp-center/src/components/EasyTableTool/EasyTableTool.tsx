import { Button } from 'antd'
import React from 'react'

type Props = {}

const EasyTableTool = (props: Props) => {
	return (
		<div className='easy-table-tool'>
			<Button type="primary">创建</Button>
		</div>
	)
}

export default EasyTableTool