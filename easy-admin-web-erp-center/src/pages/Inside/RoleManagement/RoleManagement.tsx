import React, {FC} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const RoleManagement: FC<Props> = (props) => {
    return (
        <div>RoleManagement</div>
    );
};

export default RoleManagement;
