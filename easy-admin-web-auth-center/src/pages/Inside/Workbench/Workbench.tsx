import React, {FC} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const Workbench: FC<Props> = (props) => {
    return (
        <div>Workbench</div>
    )
}

export default Workbench
