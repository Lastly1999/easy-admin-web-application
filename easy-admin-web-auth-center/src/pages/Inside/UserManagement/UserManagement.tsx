import React, {FC} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const UserManagement: FC<Props> = (props) => {
    return (
        <div>UserManagement</div>
    );
};

export default UserManagement;
