import React, {FC} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const SystemSettings: FC<Props> = (props) => {

    return (
        <div>SystemSettings</div>
    );
};

export default SystemSettings;
